name: Space Heartbeat

on:
  schedule:
    - cron: '*/15 * * * *' # Every 15 minutes
  workflow_dispatch: # Allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Hugging Face Space
        run: |
          # Replace the URL below with your actual Hugging Face Space URL
          # Format: https://<username>-<space-name>.hf.space/api/v1/health
          curl -f -s https://roshan123478-snapmindai.hf.space/api/v1/health || echo "Ping failed"
